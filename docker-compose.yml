services:
  eurovision-party:
    build: .
    container_name: eurovision-party
    ports:
      - "3000:3000"
    environment:
      - PUBLIC_HOST=${PUBLIC_HOST:-localhost}
    volumes:
      - ./data:/app/data
      - ./countries.csv:/app/countries.csv:ro
    restart: unless-stopped
